<?xml version="1.0" encoding="utf-8"?>
<!-- The collapsed balloon styled as a little screen/monitor: a dark rounded panel with a subtle
     bezel, so the floating control reads as a display the user taps into (not a chat bubble). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="#FF10151C" />
    <corners android:radius="10dp" />
    <stroke android:width="2dp" android:color="#FF3A4756" />
</shape>
