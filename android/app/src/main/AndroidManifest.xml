<?xml version="1.0" encoding="utf-8"?>
<!--
  bsdrX — Bigscreen Remote Desktop agent (Android host).
  Copyright (C) 2026 Stefy Lanza <stefy@nexlab.net>. GPLv3-or-later.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <!-- streaming a local file to the headset is a dataSync foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- webcam source: camera capture from the foreground service (Android 14+ typed FGS) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- the floating "balloon" control overlay that survives app-switching -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- AudioPlaybackCapture (system audio) requires RECORD_AUDIO -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_bubble"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bsdr">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- owns the native agent + MediaProjection + capture/audio + the balloon -->
        <service
            android:name=".BsdrService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|dataSync|camera" />

        <!-- receives marshalled input from the headset and dispatches gestures/text -->
        <service
            android:name=".BsdrAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
