# build.conf — faceswap plugin (bundles faceswap.c, which uses ONNX: SCRFD + ArcFace + inswapper).
# Cross-platform ONNX linking lives in the shared helper (Linux/Windows/macOS). Without ONNX the
# effect stubs out.
[ -f scripts/onnx-plugin-link.sh ] && . scripts/onnx-plugin-link.sh
